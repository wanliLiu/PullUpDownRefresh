// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.1'
        classpath 'com.novoda:bintray-release:0.5.0'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

project.ext {

    ANDROID_COMPILE_SDK_VERSION = 27
    ANDROID_BUILD_TOOLS_VERSION = "27.0.3"

    ANDROID_MIN_SDK_VERSION = 14
    ANDROID_TARGET_SDK_VERSION = 27

    supportPackageVersion = "27.0.2"

    //jencter
    releaseRepoName = "maven"
    releaseUserOrg = 'soli'
    releaseGroupId = 'com.soli.refresh'
    releaseVersion = '0.0.2'
    releaseWebsite = 'https://github.com/wanliLiu/PullUpDownRefresh'
}
